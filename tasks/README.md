# Task Contracts

Use `task.yaml` as the single source of truth for a run.

Minimum fields:
- goal
- acceptance (checkbox lines)
- scope

LLM usage should be explicit per task (llm_required: true/false).
